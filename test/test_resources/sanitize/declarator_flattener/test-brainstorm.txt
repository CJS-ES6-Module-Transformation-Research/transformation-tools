flatten single
    object pattern
    id
flatten double
    object pattern
    id
    cross prod
flatten triple
    mix obj patt && id

above inside for loop

above inside if

above inside for inside if
