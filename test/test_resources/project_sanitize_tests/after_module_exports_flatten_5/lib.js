
const _moduleAccess___package_export = require('./package.json.export.js');
const _moduleAccess_lodash = require('lodash');
const _moduleAccess_mocha = require('mocha');
const _moduleAccess___test_test_dat_export = require('./test/test_dat.json.export.js');
module.exports = _moduleAccess___package_export;
console.log('#!/bin/bash');
let a;
let b;
let c;
let g = [
    1,
    2,
    3
];
for (let j = a, k = _moduleAccess_lodash.includes; c > a > 0; c--) {
    console.log('hello world ');
}
for (let i = 0; i < g.length; i++) {
    for (let j = _moduleAccess_mocha, k = i * 2; 32 - k > 0;) {
        console.log(i + k);
    }
}
let x = require('./test/test_dat.json.export.js');
