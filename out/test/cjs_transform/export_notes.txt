module.exports access
(anywhere not in a test)
if(accessing object, do what?)
if(accessing property){
	perform access replace mechanism 
	OR do later with associative array 
}








should i be thinking about performace?? how much so?